pipeline {
    agent any
    options {
        skipDefaultCheckout(true)
    }

    stages {
        stage('Code checkout from GitHub') {
            steps {
                script {
                    cleanWs()
                    git credentialsId: 'github-token', url: 'https://github.com/AdamKazmierczak/abcd-student', branch: 'develop'
                }
            }
        }

        stage('ZAP Scan') {
            steps { 
                script {
                    // Use catchError to handle non-zero exit codes
                    catchError(buildResult: 'SUCCESS', stageResult: 'SUCCESS') {
                        sh 'trufflehog git file://. --branch main --fail --force-skip-archives > /tmp/abcd-student-Reports/trufle.txt'
                    }
                }
            }
        }
    }

    // post {
    //     always {
    //         defectDojoPublisher(
    //             artifact: '/tmp/abcd-student-Reports/zap_xml_report.xml',
    //             productName: 'Juice Shop',
    //             scanType: 'Trufflehog Scan',
    //             engagementName: 'adaml.kazmierczak@gmail.com'
    //         )
    //     }
   // }
}